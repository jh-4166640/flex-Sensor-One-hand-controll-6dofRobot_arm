# paho mqtt linux setting
/etc/dev/paho-mosquitto/mosquitto.conf set: allow annonymous true
host = '0,0,0,0'

$systemctl start mosquitto.service
$systemctl start mosquitto.service
$systemctl start mosquitto.service
